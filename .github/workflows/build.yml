name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always
  MACOSX_DEPLOYMENT_TARGET: 10.7

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: Build
      run: |
        rustup target add x86_64-unknown-linux-gnu
        cargo build --package kidfile-explorer --release --locked --target x86_64-unknown-linux-gnu
    - uses: actions/upload-artifact@v4
      with:
        name: kidfile-explorer
        path: target/release/kidfile-explorer
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: Build
      run: |
        rustup target add x86_64-pc-windows-gnu
        cargo build --package kidfile-explorer --release --locked --target x86_64-pc-windows-gnu
    - uses: actions/upload-artifact@v4
      with:
        name: kidfile-explorer
        path: target/release/kidfile-explorer.exe
  build-macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: Build
      run: |
        rustup target add x86_64-apple-darwin
        cargo build --package kidfile-explorer --release --locked --target x86_64-apple-darwin
    - uses: actions/upload-artifact@v4
      with:
        name: kidfile-explorer
        path: target/release/kidfile-explorer
